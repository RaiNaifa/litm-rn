/**
 * Legacy item types retained in the manifest only so old worlds can load and
 * migrate them safely. They must not be offered for creation.
 */
export const EMBEDDED_ONLY = ["backpack", "hero", "theme"];
